using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class yemSpawnerRekabetçi : Spawner

    
{   

   /* void Start()
    {
        StartCoroutine(spawnYem(time));
        yem.name = "yem";
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public IEnumerator spawnYem (float time)
    {

        while (true)
        {
            float yemPosition = Random.Range(0.93f, height);
            Instantiate(yem, new Vector3(10.5f, yemPosition , 0), Quaternion.identity);
            Instantiate(yem, new Vector3(10.5f, yemPosition-5.4f, 0), Quaternion.identity);
            yield return new WaitForSeconds(time);
        }

    }*/
}
